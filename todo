	TODOLIST de l'examen decembre 2025 par Nekena(ETU004092) et Tsiky(ETU004069)

General
    base
        Mettre une variable baseURL dans config pour faciliter le deploiement
        Creation de la base de donnees: 
        creation tables:
            vehicule
                -id
                -numero
            livreur
                -id
                -nom
                -prenom
            livraison
                -id_livraison INT PRIMARY KEY AUTO_INCREMENT,
                -date_livraison 
                -id_vehicule 
                -id_livreur 
                -id_colis 
                -adresse_depart 
                -id_zone 
                -id_statut 
                -cout_vehicule 
            statut
                -id
                -type(livré;en attente;annulée)
            zone
                -id
                -nom
            colis
            	-id
            	-nom
            	-prix
            parametre (pour mettre le prix par kilos des colis)
            	-id
            	-prix
        fonction

        design
            separer le footer et le header dans des fichiers 
            
        integration
            creation des models / controllers :
             livraison / livraisonController
             livreur / livreurControlleur
             vehicule / util
             statut / util
             zone / util
//===============================================================

1. Gestion de livraison

    _(ok)formulaire pour creer une livraison
        base    
        fonction
               (ok)insererLivraison() (par Nekena)
               (ok)getColis() (par Nekena)
               (ok)getStatut() (par Nekena)
               (ok)getVehicule() (par Nekena)
               (ok)getZone() (par Nekena)

        design
            (ok)form + input: (par Nekena et Tsiky)
                colis (liste deroulante)
                vehicule
                date de livraison
                livreur(liste deroulante)
                addresse_depart(entrepot)
                nom de zone(addresse_destination)
                statut(liste deroulante)
                cout du vehicule
                +buton valider
        integration
                (ok) Utilisation de ajax XHR pour l'insertion(par Nekena)
                (ok)appeler insererLivraison(), inserer les donnees recuperees via form (dans livraisonController) (par Nekena)
                (ok)appeler getColis(),getStatut(),getVehicule(),getZone() (dans util) (par Nekena)

    _(ok)formulaire pour modifier une livraison (Pre-rempli) et modifier
        base    
        fonction
            meme  fonction que inserer (getColis,etc,..........)
            fonction getLivraisonParId($id) (Par Nekena)
            routes pour aller vers le formulaire (avec appel de getColis,getLivreur pour les listes deroulantes)
        design
            (ok)form + input: avec les valeurs avant modification(par Nekena)
                colis (liste deroulante)
                vehicule
                date de livraison
                livreur(liste deroulante)
                addresse_depart(entrepot)
                nom de zone(addresse_destination)
                statut(liste deroulante)
                cout du vehicule
                +buton modifier
                +input type hidden de l'id de la livraison a modifier
        integration
                (ok) Utilisation de ajax XHR pour la modification(par Nekena)
                (ok)appeler updaterLivraison() avec les donnees recuperees via form (dans livraisonController) (par Nekena)

    (ok)_affichage des livraison
        base
            (ok)creation vue pour avoir les prix en fonction du poids par colis (par Tsiky)
            (ok)insertion des donnees de test pour chaque table (livraison,livreur,vehicule,statut,zone)(par Nekena )
        fonction
            (ok)getLivraison()(par Tsiky)
        design
            (ok)Affichage sous forme de tableau(par Tsiky)
        integration
            (ok)appeler getLivraison()(dans livraisonController)(par Tsiky)

//===============================================================
2.(ok) Créer directement les autres données nécessaires dans la table

3.  bénéfice par periode (jour,mois ,annee)
        base
            (ok)creation de vue  pour calcul du cout de revient (par Tsiky)
            (ok)creation de vues differentes pour calcul de chaque benefice par periode (par Tsiky)
        fonction
            (ok)getBeneficeParJour(jour) (par Tsiky)
            (ok)getBeneficeParMois(mois,annee) (par Tsiky)
            (ok)getBeneficeParAnnee(annee) (par Tsiky)
        design
            (ok)mettre un filtre pour chaque periode (par Tsiky)
            (ok)Affichage pour chaque periode (par Tsiky)
        integration
            appel des fonctions : 
                (ok)getBeneficeParJour(jour) 	}
                (ok)getBeneficeParMois(mois,annee) 	}(dans livraisonController avec getBenef()) (par Tsiky) 
                (ok)getBeneficeParAnnee(annee) 	}

Partie 2 
//===============================================================

4)modification de la base de donnee
